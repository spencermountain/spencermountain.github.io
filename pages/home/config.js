// tunables for the home page
export const triggerOffset = 0.5 // where scroll-triggers fire, as a fraction of the viewport height
export const swapDelay = 500 // ms before the tall tile follows the satellite tile's image swap
export const misc = name => `/img/misc/${name}.jpg` // small collage images
